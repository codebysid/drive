import { Skeleton } from "./ui/skeleton"

const Loader = () => {
  return (
    <Skeleton className="h-10 w-full flex justify-center items-center mb-3">Uploading File...</Skeleton>
  )
}

export default Loader
